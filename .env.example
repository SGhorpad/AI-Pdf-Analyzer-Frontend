# URL of your deployed backend (Render, Railway, etc). For local dev, leave
# this pointing at localhost — the app falls back to it automatically anyway.
VITE_API_URL=ai-pdf-analyzer-production-63a3.up.railway.app
